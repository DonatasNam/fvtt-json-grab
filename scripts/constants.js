/** Single source of truth for identifiers shared across the module. */
export const MODULE_ID = "json-grab";

/** Folder created under worlds/<world-id>/ that holds exported files. */
export const EXPORT_DIR_NAME = "json-grab";

/** Document flag key holding the per-document URL salt. */
export const FLAG_SALT = "salt";

/** Document flag key holding the timestamp (ms) of the latest share. */
export const FLAG_SHARED_AT = "sharedAt";
